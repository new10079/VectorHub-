# VectorHub — build & runtime dependencies
# Usage:  python -m venv .venv  &&  pip install -r requirements.txt
# (on Windows:  .venv\Scripts\activate   |  on Linux/macOS:  source .venv/bin/activate)

# --- Build toolchain (needed to compile the C++ HNSW extension) ---
cmake>=3.15
ninja
pybind11>=2.10
scikit-build-core>=0.3

# --- Core runtime ---
numpy>=1.19.0

# --- Testing ---
pytest
